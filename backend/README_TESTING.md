## Автоматические тесты для бэкенда

### Что тестируется:

- регистрации пользователя и ограниченный доступ
- одобрение администратором новых пользователей
- запросы на доступ к проекту и их утверждение
- конвейер обработки отчётности
- неудачная обработка неверных данных импорта
- прогнозирование шаблона ML
- отказ в доступе к "чужим" артефактам проекта

### Установка зависимостей

```python -m pip install -r requirements/base.txt -r requirements/dev.txt```

### Запуск тестов

```pytest```

В наборе тестов используется:

- База данных SQLite, автоматически созданная для тестирования
- режим локальной синхронизации
- каталог временного хранения для загрузки и экспорта
